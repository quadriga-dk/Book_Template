# these commands should be used from inside the virtual environment of the project

pip install -r nvim-requirments.txt

python -m ipykernel install --user --name=Book_Template_venv --display-name="Book_Template .venv"
